<%= form_for(@encounter_area_feature) do |f| %>
  <% if @encounter_area_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@encounter_area_feature.errors.count, "error") %> prohibited this encounter_area_feature from being saved:</h2>

      <ul>
      <% @encounter_area_feature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :encounter_id %><br />
    <%= f.text_field :encounter_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
